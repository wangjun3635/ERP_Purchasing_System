<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.team.purchasing.mapper.productcomment.ProductCommentDao">

    <select id="queryProductComment" resultType="com.team.purchasing.bean.productcomment.ProductComment">
        select id,
         productId,
         userId,
         userNickName,
         star,
         comment,
         createTime,
         createUserId,
         modifyTime,
         modifyUserId from tb_product_comment
         where productId=#{productId} order by modifyTime desc
    </select>

    <insert id="addComment">
        insert into tb_product_comment(
         productId,
         userId,
         userNickName,
         star,
         comment,
         createTime,
         createUserId,
         modifyTime,
         modifyUserId) values(#{productComment.productId},
          #{productComment.userId},
          #{productComment.userNickName},
          #{productComment.star},
          #{productComment.comment},
          GETDATE(),
          #{productComment.createUserId},
          GETDATE(),
          #{productComment.updateUserId});
    </insert>

</mapper>